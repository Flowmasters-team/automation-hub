[project]
name = "mcp-bitrix24"
version = "2.0.0"
description = "MCP-сервер для Bitrix24 — 9 доменных модулей, 200+ инструментов"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[project.scripts]
mcp-bitrix24 = "src.__main__:run_stdio_server"
